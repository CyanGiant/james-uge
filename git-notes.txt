these are notes being added for learning git branch
the command git branch will create a new branch
using git branch followed by a name will create the name of the file
git branch git_notes will create a branch called git_notes
git checkout git_notes will move to that branch and make it active
this is a new line
